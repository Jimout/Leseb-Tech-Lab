'use client'

/**
 * Deprecated: replaced by `/admin/login` + `AdminRouteGuard`.
 * Kept temporarily to avoid breaking imports in older branches.
 */
export function AdminAuthGate({ children }: { children: React.ReactNode }) {
  return children
}

