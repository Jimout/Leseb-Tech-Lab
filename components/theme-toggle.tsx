'use client'

/**
 * Semantic alias for the app theme control (next-themes, `attribute="class"` on the document root).
 * @see ThemeToggleButton
 */
export { ThemeToggleButton as ThemeToggle } from '@/components/theme-toggle-button'
